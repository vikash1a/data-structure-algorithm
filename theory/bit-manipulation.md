## Bit Manipulation

Operators
- AND (&) ->  1 if both 1
- OR (|) ->1 if atleast one has 1
-  XOR (^) -> 1 if only one 1
- NOT (~) -> Invert all bits
- Left Shift (<<) -> Shift bit to left , filling with zeros
- Right shift (>>) -> shift bit to right, filling with zeros
